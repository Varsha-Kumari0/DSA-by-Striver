#include <bits/stdc++.h>
using namespace std;

int main()
{

    // UNORDERED SET IS USED TO STORE UNIQUE VALUES
    unordered_set<int> us;
    us.insert(8); // {8}
    us.insert(4); // {4, 8}
    us.insert(2); // {2, 4, 8}
}